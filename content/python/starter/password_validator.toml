[meta]
id = "py-starter-password-validator"
zone = "starter"
language = "python"
tags = ["strings", "validation", "functions"]
difficulty = 2

[code]
content = """
# Password Validator

# VIM TIP: Use '%' to jump between matching brackets.

def check_length(password):
    return len(password) >= 8

def has_uppercase(password):
    for char in password:
        if char.isupper():
            return True
    return False

def has_lowercase(password):
    for char in password:
        if char.islower():
            return True
    return False

def has_digit(password):
    for char in password:
        if char.isdigit():
            return True
    return False

def validate_password(password):
    errors = []
    if not check_length(password):
        errors.append("Must be at least 8 characters")
    if not has_uppercase(password):
        errors.append("Must contain an uppercase letter")
    if not has_lowercase(password):
        errors.append("Must contain a lowercase letter")
    if not has_digit(password):
        errors.append("Must contain a digit")
    return errors

# Test passwords
test_passwords = ["abc", "abcdefgh", "Abcdefgh", "Abcdefg1"]
for pwd in test_passwords:
    issues = validate_password(pwd)
    if issues:
        print(f"'{pwd}' - WEAK: {', '.join(issues)}")
    else:
        print(f"'{pwd}' - STRONG password!")
"""

[[tasks]]
type = "move_to"
anchor = { pattern = "validate_password", occurrence = 1 }
description = "Move to 'validate_password'"
points = 50
optimal_keys = 5

[[tasks]]
type = "move_to"
anchor = { pattern = "STRONG", occurrence = 1 }
description = "Move to 'STRONG'"
points = 75
optimal_keys = 8
