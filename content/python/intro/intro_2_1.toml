[meta]
id = "intro-2-1-insert-basics"
zone = "basic_edit"
language = "python"
tags = ["intro", "insert", "i", "a"]
difficulty = 1
intro = true
intro_level = "2-1"

[code]
content = """
# ===================================
# WORLD 2: Basic Edit
# ===================================
#
# Split screen! Right side shows the
# target code. Left side is yours.
#
# Add the missing parts using:
#   i -- insert before cursor
#   a -- insert after cursor
#   Esc -- back to normal mode
#
# Type the actual code. Energy drains
# over time -- be quick!
# ===================================

def fibonacci(n):
    if n <= 0:
        return []
    if n == 1:
        return [0]
    fib = [0, 1]
    for i in range(2, n):
        next_val = fib[i-1] + fib[i-2]
        fib.append(next_val)
    return fib
"""

[[removals]]
type = "inline"
line_anchor = "if n <= 0"
remove = "<= 0"
description = "Add '<= 0' comparison (use a after n)"
entry_point = "a"
points = 50

[[removals]]
type = "inline"
line_anchor = "return []"
remove = "[]"
description = "Add empty list '[]' (use a after return)"
entry_point = "a"
points = 50

[[removals]]
type = "inline"
line_anchor = "fib = [0, 1]"
remove = "[0, 1]"
description = "Add '[0, 1]' (use a after =)"
entry_point = "a"
points = 75

[[removals]]
type = "inline"
line_anchor = "fib.append"
remove = "next_val"
insert_text = "next_val"
description = "Add 'next_val' argument (use i before ))"
entry_point = "i"
points = 75
