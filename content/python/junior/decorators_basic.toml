[meta]
id = "py-junior-decorators-basic"
zone = "junior"
language = "python"
tags = ["decorators", "functions", "wraps"]
difficulty = 3

[code]
content = """
import functools
import time


def timer(func):
    \"\"\"Measure execution time of a function.\"\"\"
    @functools.wraps(func)
    def wrapper(*args, **kwargs):
        start = time.perf_counter()
        result = func(*args, **kwargs)
        elapsed = time.perf_counter() - start
        print(f"{func.__name__} took {elapsed:.4f}s")
        return result
    return wrapper


def retry(max_attempts=3):
    \"\"\"Retry a function up to max_attempts times on failure.\"\"\"
    def decorator(func):
        @functools.wraps(func)
        def wrapper(*args, **kwargs):
            for attempt in range(1, max_attempts + 1):
                try:
                    return func(*args, **kwargs)
                except Exception as exc:
                    if attempt == max_attempts:
                        raise
                    print(f"Attempt {attempt} failed: {exc}")
        return wrapper
    return decorator


@timer
def slow_sum(numbers):
    total = sum(numbers)
    time.sleep(0.1)
    return total
"""

[[tasks]]
type = "move_to"
anchor = { pattern = "functools", occurrence = 1 }
description = "Move to 'functools'"
points = 75
optimal_keys = 3

[[tasks]]
type = "move_to"
anchor = { pattern = "max_attempts", occurrence = 1 }
description = "Move to 'max_attempts'"
points = 100
optimal_keys = 5

[[tasks]]
type = "move_to"
anchor = { pattern = "elapsed", occurrence = 1 }
description = "Move to 'elapsed'"
points = 100
optimal_keys = 6
