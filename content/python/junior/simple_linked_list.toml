[meta]
id = "py-junior-simple-linked-list"
zone = "junior"
language = "python"
tags = ["linked-list", "data-structures", "nodes"]
difficulty = 4

[code]
content = """
class Node:
    \"\"\"A single node in a linked list.\"\"\"

    def __init__(self, value, next_node=None):
        self.value = value
        self.next_node = next_node

    def __repr__(self):
        return f"Node({self.value})"


class LinkedList:
    \"\"\"A singly linked list implementation.\"\"\"

    def __init__(self):
        self.head = None
        self.length = 0

    def prepend(self, value):
        new_node = Node(value, self.head)
        self.head = new_node
        self.length += 1

    def append(self, value):
        new_node = Node(value)
        if self.head is None:
            self.head = new_node
        else:
            current = self.head
            while current.next_node:
                current = current.next_node
            current.next_node = new_node
        self.length += 1

    def find(self, value):
        current = self.head
        while current:
            if current.value == value:
                return current
            current = current.next_node
        return None

    def to_list(self):
        result = []
        current = self.head
        while current:
            result.append(current.value)
            current = current.next_node
        return result


linked = LinkedList()
for item in [10, 20, 30, 40]:
    linked.append(item)
"""

[[tasks]]
type = "move_to"
anchor = { pattern = "LinkedList", occurrence = 1 }
description = "Move to 'LinkedList'"
points = 75
optimal_keys = 4

[[tasks]]
type = "move_to"
anchor = { pattern = "prepend", occurrence = 1 }
description = "Move to 'prepend'"
points = 100
optimal_keys = 5

[[tasks]]
type = "move_to"
anchor = { pattern = "to_list", occurrence = 1 }
description = "Move to 'to_list'"
points = 100
optimal_keys = 6
