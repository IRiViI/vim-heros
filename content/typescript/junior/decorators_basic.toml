[meta]
id = "ts-junior-decorators-basic"
zone = "junior"
language = "typescript"
tags = ["decorators", "experimental"]
difficulty = 4

[code]
content = """
function LogMethod(
  target: any,
  propertyKey: string,
  descriptor: PropertyDescriptor
): PropertyDescriptor {
  const original = descriptor.value;
  descriptor.value = function (...args: any[]) {
    console.log(`Calling ${propertyKey} with args:`, args);
    const result = original.apply(this, args);
    console.log(`${propertyKey} returned:`, result);
    return result;
  };
  return descriptor;
}

function Sealed(constructor: Function): void {
  Object.seal(constructor);
  Object.seal(constructor.prototype);
}

function ReadOnly(
  target: any,
  propertyKey: string,
  descriptor: PropertyDescriptor
): PropertyDescriptor {
  descriptor.writable = false;
  return descriptor;
}

@Sealed
class Calculator {
  private history: string[] = [];

  @LogMethod
  add(a: number, b: number): number {
    const result = a + b;
    this.history.push(`${a} + ${b} = ${result}`);
    return result;
  }

  @LogMethod
  multiply(a: number, b: number): number {
    const result = a * b;
    this.history.push(`${a} * ${b} = ${result}`);
    return result;
  }

  @ReadOnly
  getHistory(): string[] {
    return [...this.history];
  }
}

const calc = new Calculator();
calc.add(3, 5);
calc.multiply(4, 7);
"""

[[tasks]]
type = "move_to"
anchor = { pattern = "LogMethod", occurrence = 1 }
description = "Move to 'LogMethod'"
points = 75
optimal_keys = 5

[[tasks]]
type = "move_to"
anchor = { pattern = "Calculator", occurrence = 1 }
description = "Move to 'Calculator'"
points = 100
optimal_keys = 8

[[tasks]]
type = "move_to"
anchor = { pattern = "getHistory", occurrence = 1 }
description = "Move to 'getHistory'"
points = 100
optimal_keys = 7
